{
 "cells": [
  {"cell_type": "markdown", "source": ["# HopeAI v0.8 训练 - Kaggle GPU\n微调 Qwen2.5-0.5B 作为网元模型的知识引擎\n\n运行: Accelerator GPU T4 x2, 数据上传到 /kaggle/input/hopeai-data/"]},
  {"cell_type": "code", "source": ["!pip install -q transformers datasets accelerate peft bitsandbytes trl torch"], "outputs": []},
  {"cell_type": "code", "source": ["import os, json, torch\nfrom datetime import datetime\nfrom transformers import AutoTokenizer, AutoModelForCausalLM, TrainingArguments, Trainer, DataCollatorForSeq2Seq, BitsAndBytesConfig\nfrom datasets import Dataset\nfrom peft import LoraConfig, get_peft_model, TaskType, PeftModel"], "outputs": []},
  {"cell_type": "code", "source": ["# 加载数据\ntry:\n    with open('/kaggle/input/hopeai-data/train.json') as f: train_data = json.load(f)\n    with open('/kaggle/input/hopeai-data/val.json') as f: val_data = json.load(f)\n    print(f'本地: train={len(train_data)} val={len(val_data)}')\nexcept:\n    from datasets import load_dataset\n    ds = load_dataset('BelleGroup/train_0.5M_CN', split='train[:8000]')\n    train_data = [{'instruction':i.get('instruction',''),'input':i.get('input',''),'output':i.get('output','')} for i in ds]\n    train_data, val_data = train_data[:7000], train_data[7000:]\n    print(f'HuggingFace: train={len(train_data)} val={len(val_data)}')"], "outputs": []},
  {"cell_type": "code", "source": ["def fmt(item):\n    inst, inp, out = item.get('instruction',''), item.get('input',''), item.get('output','')\n    text = f\"<|im_start|>user\\n{inst}\\n{inp}<|im_end|>\\n<|im_start|>assistant\\n{out}<|im_end|>\" if inp else f\"<|im_start|>user\\n{inst}<|im_end|>\\n<|im_start|>assistant\\n{out}<|im_end|>\"\n    return {'text': text}\n\ntrain_ds = Dataset.from_list([fmt(d) for d in train_data])\nval_ds = Dataset.from_list([fmt(d) for d in val_data])\nprint(f'DS: {len(train_ds)}/{len(val_ds)}')"], "outputs": []},
  {"cell_type": "code", "source": ["MODEL = 'Qwen/Qwen2.5-0.5B-Instruct'\nprint(f'加载: {MODEL}')\n\ntok = AutoTokenizer.from_pretrained(MODEL, trust_remote_code=True)\ntok.pad_token = tok.eos_token\n\nbnb = BitsAndBytesConfig(load_in_4bit=True, bnb_4bit_quant_type='nf4', bnb_4bit_compute_dtype=torch.float16, bnb_4bit_use_double_quant=True)\nmodel = AutoModelForCausalLM.from_pretrained(MODEL, quantization_config=bnb, device_map='auto', trust_remote_code=True, torch_dtype=torch.float16)\nmodel.config.use_cache = False"], "outputs": []},
  {"cell_type": "code", "source": ["def tok_fn(ex):\n    r = tok(ex['text'], truncation=True, max_length=512, padding=False)\n    r['labels'] = r['input_ids'].copy()\n    return r\n\ntok_train = train_ds.map(tok_fn, remove_columns=train_ds.column_names)\ntok_val = val_ds.map(tok_fn, remove_columns=val_ds.column_names)\nprint(f'Tokenized: {len(tok_train)}/{len(tok_val)}')"], "outputs": []},
  {"cell_type": "code", "source": ["lora = LoraConfig(task_type=TaskType.CAUSAL_LM, r=8, lora_alpha=16, lora_dropout=0.05,\n    target_modules=['q_proj','k_proj','v_proj','o_proj','gate_proj','up_proj','down_proj'], bias='none')\nmodel = get_peft_model(model, lora)\nmodel.print_trainable_parameters()"], "outputs": []},
  {"cell_type": "code", "source": ["out = '/kaggle/working/hopeai_qwen05b_lora'\nargs = TrainingArguments(output_dir=out, num_train_epochs=3, per_device_train_batch_size=4, per_device_eval_batch_size=4,\n    gradient_accumulation_steps=4, warmup_ratio=0.03, learning_rate=2e-4, logging_steps=10, save_steps=200, eval_steps=200,\n    save_total_limit=3, load_best_model_at_end=True, metric_for_best_model='eval_loss', fp16=True,\n    gradient_checkpointing=True, optim='paged_adamw_8bit', report_to='none', remove_unused_columns=False)\n\ndc = DataCollatorForSeq2Seq(tokenizer=tok, model=model, padding=True)\nprint(f'输出: {out}')"], "outputs": []},
  {"cell_type": "code", "source": ["print(f'=== 训练开始 {datetime.now().strftime(\"%Y-%m-%d %H:%M:%S\")} ===')\ntrainer = Trainer(model=model, args=args, train_dataset=tok_train, eval_dataset=tok_val, data_collator=dc)\ntrainer.train()"], "outputs": []},
  {"cell_type": "code", "source": ["model.save_pretrained(out)\ntok.save_pretrained(out)\neval_r = trainer.evaluate()\nwith open(f'{out}/training_log.json','w') as f:\n    json.dump({'eval':eval_r,'train_n':len(train_data),'val_n':len(val_data),'model':MODEL,'done':datetime.now().isoformat()},f,indent=2)\nprint(f'保存: {out}')\nprint(f'Loss: {eval_r}')"], "outputs": []},
  {"cell_type": "code", "source": ["print('\\n=== 推理测试 ===')\nmodel.eval()\nfor q in ['人工智能是什么？','怎么学Python？','什么是知识图谱？']:\n    p = f'<|im_start|>user\\n{q}<|im_end|>\\n<|im_start|>assistant\\n'\n    inp = tok(p, return_tensors='pt').to(model.device)\n    with torch.no_grad():\n        o = model.generate(**inp, max_new_tokens=128, temperature=0.7, do_sample=True, top_p=0.9)\n    r = tok.decode(o[0], skip_special_tokens=True).split('<|im_start|>assistant\\n')[-1].replace('<|im_end|>','').strip()\n    print(f'Q: {q}')\n    print(f'A: {r[:150]}')\n    print('---')"], "outputs": []}
 ]
}
